version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: cfar_engine
      POSTGRES_USER: cfar
      POSTGRES_PASSWORD: cfar_local
    ports:
      - "5432:5432"
